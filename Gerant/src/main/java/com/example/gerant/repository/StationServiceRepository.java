package com.example.gerant.repository;

import com.example.gerant.entity.StationService;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StationServiceRepository extends JpaRepository<StationService, Integer> {
}
